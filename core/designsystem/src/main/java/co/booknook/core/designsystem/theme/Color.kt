package co.booknook.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// ── Light mode palette ──────────────────────────────────────────────────────
val Cream = Color(0xFFF5F0E8)
val DarkBrown = Color(0xFF1A1512)
val WarmBrown = Color(0xFF8B7355)
val SoftWhite = Color(0xFFFEFCF9)
val AccentGreen = Color(0xFF2D6A4F)
val Terracotta = Color(0xFFB05C3A)
val MutedTerracotta = Color(0xFFD4876B)

// ── Dark mode palette (deep charcoal) ──────────────────────────────────────
val Charcoal = Color(0xFF0F0F0F)        // page background
val DeepCharcoal = Color(0xFF1C1C1E)    // card / surface

// ── Shared semantic aliases ────────────────────────────────────────────────
val SurfaceColor = Color(0xFFFEFCF9)
val OnSurfaceColor = Color(0xFF1A1A1A)
val Accent = AccentGreen
val Secondary = WarmBrown
